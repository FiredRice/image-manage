import react_middleware from './react';
import antd_middleware from './antd';
import global_middleware from './global';

export default [
    react_middleware,
    antd_middleware,
    global_middleware
];